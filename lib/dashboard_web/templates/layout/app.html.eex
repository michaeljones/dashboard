<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Dashboard</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header class="bg-dark-blue text-white p-3 sm:px-8 sm:text-center sm:flex sm:items-center">
      <div class="w-32 flex-grow-0 text-left sm:ml-4">
        <%= if @conn.assigns[:list_name] do %>
          <a href="/">Dashboard</a>
        <% end %>
      </div>
      <%= if @conn.assigns[:list_name] do %>
        <h1 class="text-3xl flex-grow"><%= @list_name %></h1>
      <% else %>
        <h1 class="text-3xl flex-grow">Dashboard</h1>
      <% end %>
      <div class="w-32 flex-grow-0">
        <a href="https://github.com/michaeljones/dashboard">Github</a>
      </div>
    </header>
    <main role="main" class="sm:p-8">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
    <footer class="bg-dark-blue text-white p-4 text-center">Version: 2020.9.4+0</footer>
  </body>
</html>
